# Example config for mirror-pdf-drive.
# Copy this file to mirror-pdf-drive.config.yaml and adjust paths.

version: 1

source:
  # Directory whose .md files will be converted.
  root: ./Documents-es
  include:
    - "**/*.md"
  exclude: []
  max_depth: 10

output:
  # Directory where PDFs are written. Created if missing.
  root: ./dist/mirror-pdf-drive
  clean: false

render:
  html_template: null
  # Custom CSS file (overrides the auto-generated typography CSS below).
  css_file: null
  page_size: A4
  margins:
    top: 2.0
    bottom: 2.0
    left: 2.0
    right: 2.0
  metadata:
    title: Documents-es
    author: Sebastián Illa

  # Tipografía. El CLI genera un CSS con estos defaults si css_file
  # no está set. Inter y Roboto como preferred, fallback a fonts
  # del sistema. Los colores están validados para WCAG AA en blanco.
  # El CSS NO sobrescribe colores inline que pandoc pone desde MD
  # (ej. <span style="color: #abc">); los colores originales del MD
  # se preservan.
  font_family: "Inter, Roboto, Helvetica, Arial, sans-serif"
  # Path a un .ttf/.otf para embeber via @font-face. Si está set,
  # se carga antes que font_family.
  font_file: null
  body_color: "#1a1a1a"   # near-black, 16.5:1 contrast
  heading_color: "#000000" # pure black, 21:1 contrast
  link_color: "#0563c1"    # Office blue, 7.4:1 contrast
  code_color: "#1a1a1a"
  code_bg_color: "#f6f8fa" # GitHub-style very light gray

drive:
  # Fixed root folder in Google Drive where all projects are mirrored.
  # The CLI creates a subfolder named `project_folder_name` (or
  # `Path.cwd().name` if not set) inside this root, then mirrors
  # the source tree structure under it.
  # Recommended: a dedicated folder like "Mirror PDFs" in your Drive.
  root_folder_id: null
  # Legacy single-folder target. Kept for backwards compatibility.
  # If both are set, root_folder_id wins.
  folder_id: null
  folder_name: Documents-es PDFs
  # Optional override for the Drive subfolder name. If not set, the
  # CLI uses Path.cwd().name (e.g. "gastos-personales" if you run
  # mirror-pdf-drive from the project root).
  project_folder_name: null
  # One of: skip, replace, version.
  conflict_strategy: skip

auth:
  # Directory where client_secret.json and token.json live.
  # Defaults to ~/.config/mirror-pdf-drive/ when omitted.
  dir: null
  client_secret_file: client_secret.json
  token_file: token.json
